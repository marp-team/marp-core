name: GitHub Release

on:
  push:
    tags:
      - v*

jobs:
  github-release:
    runs-on: ubuntu-slim
    steps:
      - uses: actions/checkout@v6
      - uses: marp-team/actions@v1
        with:
          task: release
          token: ${{ secrets.GITHUB_TOKEN }}
